{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build-example": "bower link && cd example && bower link purescript-dom-keyboard && bower install && pulp browserify --to app.js",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^10.0.0",
    "purescript": "^0.10.2"
  }
}
